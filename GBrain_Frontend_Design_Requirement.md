# GBrain 系统架构与前台设计需求 (供 Manus 自由设计)

你好，Manus！我们正在构建一个被称为 **“人机协同全栈飞轮 (Human-in-the-loop Full-stack Flywheel)”** 的多智能体 (Multi-Agent) 协作系统。

我们需要你来**从零开始设计并开发系统的前台 (Frontend)**。我们不会限制你使用任何特定的前端框架或技术栈，你可以自由发挥。但在此之前，你需要深刻理解整个系统的后端 Agent 架构，以及前台在这个体系中所必须承担的交互边界。

---

## 1. 整体 Agent 架构全景

整个系统分为几个明确的边界，前台的作用是作为唯一的人机交互入口，去驾驭后端的复杂推演。

*   **GBrain 知识库**：系统的底层真理库。只存“确定的事实和法则”，不参与聊天和流程编排。
*   **中台铁三角 (Virtual Product Team)**：核心推理引擎。每次任务会在后端拉起一个沙盒，由 **PM（主笔）**、**Tech（架构挑战）**、**QA（异常施压）** 三个 Agent 进行多轮激烈的方案攻防，最后由 **Reviewer** 执行质量门禁。
*   **Diff 经验飞轮**：系统越用越聪明的引擎。从“AI 最终草稿”与“人类最终修改稿”的差异中，提炼出可复用的经验法则。

---

## 2. 前台 (Frontend) 的核心定位

在上述架构中，前台是**共创客户端与实时仲裁室**。
我们希望你设计的前台能打破传统 AI 对话界面的“黑盒感”，让用户觉得他们是在指挥一个专业的虚拟研发团队。

**【必须遵守的架构红线】**
1. **纯粹的交互端**：前台不要做任何 LLM API 的直接调用与推理。
2. **知识隔离**：前台不能直接把用户的输入无脑写入底层知识库，所有知识沉淀必须经过“人工审核”。

---

## 3. 需要你自由设计的核心前台模块

你需要为用户设计以下 5 个核心的产品体验场景。请自由发挥你的 UI/UX 创意（例如考虑使用玻璃拟物化、深色模式、流畅的过渡动画等），重点在于让这个“复杂的工作流”变得极具掌控感。

### 模块一：任务启动工作台 (Task Launcher)
*   **需要做什么**：收集用户本次想要解决的业务目标、上传参考材料、设置某些最高优先级的强制约束。
*   **体验目标**：让用户有一种“立项并一键派发给团队”的仪式感。

### 模块二：群聊直播区 (Sandbox Live-streaming)
*   **需要做什么**：实时直播后端 PM、Tech、QA 之间的攻防讨论。后端的 Event Stream 会不断推送各个角色的发言、轮次进展等状态。
*   **需要设计的交互**：用户在看直播时，如果觉得 AI 的讨论方向错了，需要能点击 **“打断 (Interrupt)”**，强行给出一个纠偏指令让 Agent 重新推演。

### 模块三：死锁仲裁面板 (Arbitration Panel)
*   **需要做什么**：当后端的 Agent 们互相说服不了对方（触发 3 轮死锁）时，后端会抛出一个“分歧包”。
*   **需要设计的交互**：前台需要通过极具视觉冲击力的方式弹出这个面板，清晰列出**争议点是什么**，展示几个候选方案各自的**收益、代价与风险**，等待人类用户作为老板来 **“一锤定音 (Arbitration)”**。人类拍板后，沙盒继续运转。

### 模块四：共创编辑器 (Co-creation Editor)
*   **需要做什么**：当 AI 团队终于通过了门禁，输出了最终的方案草案。前台需要提供一个舒适的编辑区域，让用户审阅、亲自手动修改草案。
*   **需要设计的交互**：当用户点击“保存终稿”时，系统会在后台悄悄启动 Diff 对比。

### 模块五：法则审核页 (Rule Auditing)
*   **需要做什么**：后台 Diff 流程从用户的修改中提炼出了一些“候选法则”（比如：AI 发现用户总是喜欢加上“熔断降级策略”）。
*   **需要设计的交互**：前台像收件箱一样展示这些法则。用户可以点击 `批准入库`、`修改后入库`、`驳回`，或者限定它 `只在特定业务生效`。这是把隐性经验转化为系统长期智慧的最后一道门槛。

---

## 4. 期待你的发挥

你可以自由选择最适合实现这种**高频实时通信**、**复杂状态管理**和**高级视觉动效**的前端框架（例如 React, Vue, Svelte 等）。
请先在脑海中构思这套界面的交互骨架，然后开始你的全栈前端设计与开发！
